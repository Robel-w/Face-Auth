import streamlit as st

def app():
    st.header("Login")
    st.write("Authenticate using face recognition.")
    if st.button("Authenticate"):
        st.info("Authentication flow not implemented in template.")
